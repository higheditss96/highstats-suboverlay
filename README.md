# Highstats Sub Overlay

Overlay OBS transparent pentru afișarea Sub Goal-ului Kick (subs + gifted).

## Setup

1. `npm install`
2. `npm start`

## Build pentru Vercel
`npm run build`

### URL parametri
- `user=USERNAME`
- `color=%23ff00ff`
- `font=Poppins`
- `useGoal=true`
- `goal=100`
